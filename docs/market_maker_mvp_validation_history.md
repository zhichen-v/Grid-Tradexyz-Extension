# Market Maker MVP 驗證歷史索引

本檔保存 `market_maker_mvp_operating_guide.md` 的歷次本地驗證索引，讓主操作指南只保留目前可執行規則與最新候選。表內帳戶、餘額、持倉、訂單、fee、config SHA與測試數字都是**當時快照**，不得作為目前狀態或下一輪 preflight 的替代品。

原 `12.1–12.19` 的完整逐輪原文可由已push的commit讀取：

```powershell
git show 7c04fd224fe2eceb3dc1f42051c751eac5a748c1:docs/market_maker_mvp_operating_guide.md
```

目前判定、現行候選與下一步只看主指南第 8 節及 fresh authenticated reads。

## 稽核索引

| 原節 | 日期／階段 | 判定 | 保留重點 |
|---|---|---|---|
| 12.1 | 2026-08-20 初版修復 | Historical completion | 新增單邊模式、raw-spread guard、shutdown／stale／uncertainty fail-closed、WS lifecycle及position sign修復；當時離線測試數字不是現行baseline。 |
| 12.2 | 第一次Step B live | **NO-GO** | WS position `sign=-1`被錯讀，reduce-only bid約6秒後被錯誤safety cancel；無fill，postflight orders 0。 |
| 12.3 | 修復後Step B dry-run | **Dry safety GO** | `bid_only`約1824秒、`361/361` cycles；position方向與exposure正確、真實mutation 0；當時仍保留既有short，快照不可當現在。 |
| 12.4 | Step C歷史baseline | Historical baseline | `4000 ticks / raw guard 100 bps / 2 mutations/min`等保守值；authenticated歷史交叉驗證maker/taker fee約`0.000120/0.000350`。 |
| 12.5 | 未完成清單 | Superseded checklist | 記錄當時Step D/E、live proof與VPS缺口；後續結果由12.10–12.19及現行指南取代。 |
| 12.6 | 恢復順序 | Superseded procedure | 記錄當時Step D重新授權、fresh pre/postflight、監控與一次一參數規則；通用安全規則已整理回主指南。 |
| 12.7 | 2026-08-20 flat恢復 | **Dry safety GO / live eligibility NO-GO** | Flat exact-config dry-run約1805秒無mutation；raw spread高於當時25bps gate，因此未live。 |
| 12.8 | Spread配對校準、第二次Step B | **NO-GO** | `4000 ticks / raw guard 100 bps` dry smoke通過；live bid後因錯誤PTY pipeline未走正常shutdown且該單其後成交，留下long 0.00020。 |
| 12.9 | 殘倉處置、Step B retry、Step C | **Step B GO / Step C GO** | 授權後一次reduce-only IOC平倉；direct-PTY Step B完成create/cancel/flat proof；Step C雙邊極小額驗證完成。 |
| 12.10 | 高換手候選 | **Dry GO / first live NO-GO** | 候選改為`500 ticks / reprice 250 / raw 30 / lifetime 30s / 8 mutations/min`；首次live出現fast-fill uncertainty。 |
| 12.11 | 2026-08-23 fast-fill修復 | **T3 GO** | MM latch/reconciliation與paused status freshness修復；切到隔離測試sub-account；exact-config 30m dry-run通過。 |
| 12.12 | Step D首次高換手live | **NO-GO / stopped early** | 約5m15s；4 fills、short到cap；1次ambiguous cancellation及risk-reduction mutation阻擋。依當輪授權一次reduce-only IOC後flat。 |
| 12.13 | Step D事故修復 | **Offline + T3 GO** | 修復stale-active cancellation read與risk-reduction emergency create優先權；deterministic tests及30m dry-run通過。 |
| 12.14 | Step D live retry | **NO-GO / stopped early** | 約28m55s；position自然回flat且risk side正常，但2次cancel terminal visibility超出`2 / 0.25s`窗口；postflight flat。 |
| 12.15 | Cancellation visibility修復 | **Offline + T3 GO** | Cancellation-only read window改為`4 / 0.5s`且不重送signer mutation；deterministic tests與30m dry-run通過；獨立monitor DNS fault有bounded紀錄。 |
| 12.16 | Step E E0/E1 | **T3 GO；首次live safety NO-GO / economic INCONCLUSIVE** | Fee truth調為maker`0.000120`，half-spread縮至250ticks；live monitor跨endpoint分類及stop propagation有缺口，2筆maker只完成開倉，留下short 0.00040。 |
| 12.17 | Long-run前候選 | **Offline candidate** | 將account audit與hard-stop propagation內建；建立authenticated trade ledger、fee/net/equity gate與嚴格`1x cross`／ownership驗證。 |
| 12.18 | 最後候選T3 | **Software/safety GO** | 修復integrator-fee `null` capability邊界；約1872秒、`360/360` cycles，audit healthy、真實mutation 0；不代表economic GO。 |
| 12.19 | 4小時live long-run | **Runtime/safety GO / economic INCONCLUSIVE / volume NOT MET** | 4h穩定、3 maker fills、turnover`46.993860`、最後short 0.00020；後續證明舊MM取`levels[0]`造成假性寬BBO，因此其spread／eligible／流動性解讀已失效，但成交、cleanup與資源數據仍是歷史證據。 |
| 12.20 | MM-only BBO 正規化與 T3 | **Software/data safety GO** | MM boundary 以有效價量重建真正 BBO；30m T3 `354/354` cycles、eligible `99.84%`，未修改 shared Lighter/Grid。 |
| 12.21 | BBO 修復後 E1 canary | **Volume improved / economic NO-GO** | 約224秒、8 maker fills、turnover `126.892880`；completed net `+0.0230789 bps` 低於當時 `+0.10 bps` 門檻。 |
| 12.22 | Fee-aware exit floor | **Observed-path positive / formal inconclusive** | 20 maker fills、turnover `308.517560`；partial residual 使 formal completed gate 停在6，促成 exact-residual／oversized reduce-only 後續工作。 |
| 12.23 | Exact-residual live | **NO-GO / reverted** | Lighter 以 `21706 invalid order base or quote amount` 拒絕 sub-min reduce-only；策略依 uncertainty hard-stop，未重送。 |
| 12.24 | Oversized reduce-only 修復 | **Offline/regression GO** | 小殘倉仍提交 exchange minimum、只允許成交到 flat；final audit 可觀察。人工授權 cleanup 證實 exchange semantics。 |
| 12.25 | Oversized 修復後短 E1 | **Safety GO / economic inconclusive** | 原250-tick邊界約916秒，2 maker sells 到 short cap；只有 reduce-only buy，未自然 flat，停止後 orders 0。 |
| E2a | 250-tick 快速樣本 | **Positive signal / below formal sample gate** | 14 completed maker fills、約344秒、turnover `187.833580`、net `+0.0078799704`、`+0.4195187250 bps`、cover `1.3495989375`、DD `0.030884`。 |
| E2b | 250-tick 延伸樣本 | **Positive signal / terminal proof complete** | turnover `344.023420`、net `+0.0143371896`、`+0.4167503945 bps`、cover `1.3472919954`；exact terminal proof `unresolved=0`。 |
| E2c | 200-tick canary | **NO-GO / rollback 250** | 29 completed、turnover `509.555250`、net `+0.0015133700`、`+0.0296998216 bps`、cover `1.0247498513`、DD `0.048974`、約80–100 fills/h；經濟餘裕過薄，且舊 `3×0.5s` read retry 在 CloudFront 502 burst 後 fail-closed。 |
| E2d | Account-read retry與soft-exit信任修復 | **Offline/regression GO；live path observed** | 讀取改為 `5×1s`、外層 audit timeout 仍為10秒；soft-exit 必須等待最新 processed fill 之後的新 authenticated audit，startup retry 重新建立 baseline。後續live各吸收1次瞬時read failure，未形成consecutive failure；加入降低庫存側create優先測試後完整MM為`278/278`，full repo `515`維持既知Grid baseline `8F+4E`。 |
| E2e | 200-canary受控cleanup | **Recovery／terminal proof GO** | 只掛 `POST_ONLY + reduce_only BUY 0.00040 @ 78529.5`，authenticated maker/tick120成交，fee `0.003769416`；兩次postflight皆flat／orders0／unresolved0，collateral `299.555125`。 |
| E2f | 250-tick正式經濟場次（方向優先修復前） | **Safety／economic GO；後續由E2g取代fingerprint** | 約1195秒，32 completed／12round trips，turnover `596.629560`、exact fee `0.07159554720`、net `+0.00160445280`、`+0.0268919428 bps`、cover `1.02240995`、約 `1800 USDG/h`／`96.5 fills/h`、DD `0.033816`；一次exact-reconciled cancellation race及一次瞬時read failure，terminal unresolved0。此場暴露mutation緊張時固定BUY-first的方向問題。 |
| E2g | 250-tick／8 mutations + 降低庫存側優先 | **Previous-fingerprint short-session safety／economic GO** | 約1759秒，38 completed／15round trips，turnover `690.898688`、exact fee `0.08290784256`、net `+0.00206015744`、`+0.0298185172 bps`、cover `1.02484876`、flat-equity `+0.0298162384 bps`、約 `1416 USDG/h`／`77.9 fills/h`、DD `0.031468`；failed／unknown／unresolved／reconciliation failure皆0，一次read failure由bounded retry吸收，雙postflight乾淨。 |
| E2h | 250-tick／10 mutations單變因canary與操作者停止 | **吞吐比較INCONCLUSIVE；完整場次economic NO-GO；reverted to 8** | 約650秒；停止前22 completed／10round trips，completed turnover `345.085000`、net `+0.00126980000`、`+0.0367967312 bps`、cover `1.03066394`；runtime wall-rate（含第23筆open authenticated fill）約 `2005 USDG/h`／`127.8 fills/h`，DD `0.015979`。一筆cancel曾缺terminal proof，兩個cycle fail-closed後exact sync解除，無duplicate／unknown／reconciliation failure。操作者在30筆前要求停止，停止窗口留下short `0.00020`；maker reduce-only `78517.7`回補後，authenticated trade-ledger全場turnover `376.491440`、net `-0.00313897280`、`-0.0833743471 bps`、cover `0.93052138`；baseline／final equity `299.558978 / 299.555839`，與ledger只差`2.72e-8`。雙postflight為0/0/0；10尚無promotion證據，不能宣稱已證明10較差。 |
| E2i | REST fill-generation邊界審查與暫停交接 | **P0 deferred；未納入checkpoint** | `position_refresh_required` 同時包含純cancel與visible fill：全套用會造成soft／hard reprice ping-pong，只在full sync套用又會漏掉reconcile-confirmed fill。匆忙實驗已撤回；下一輪須拆出精確 `fill_observed`並補同淨倉位fills／純reprice反例後才能進long-run。帳戶維持flat／orders0／unresolved0，P1雙inventory timer亦待統一。 |
| E2j | P0／P1完成與正式T3 | **Offline + dry safety GO** | 精確拆分`fill_observed`與純position refresh，REST／WS／immediate-create replay只計新增fill；RiskManager成為inventory age／soft-exit latch唯一來源。Cross-review先捕獲WS replay重複signal並中止約33秒pre-T3（mutation0）；修復後fingerprint `6F792110...A538060`，MM `283/283`、full `520`維持`8F+4E`。正式T3約20:10:58–20:41:14，`344/344` cycles、`would_place=688`、真實create/cancel0，failed／reconnect／ambiguity／reconciliation failure／unknown／429皆0，flat且equity `299.555839`。 |
| E2k | P0／P1後第一短live與terminal replay tombstone | **Hard-safety NO-GO；follow-up offline GO** | 約9分鐘完成10筆並自然flat，net約`+0.002021`、`+0.1217 bps`、cover `1.1014`、max DD `0.052698`；一筆已終結managed order的延遲active replay被誤計unknown `1`，依gate停止。雙postflight flat／orders0，equity `299.557860`。加入僅對已證明terminal order生效的10秒tombstone，逾時或陌生ID仍fail closed；精準`3/3`、order manager`72/72`、MM`286/286`、full`523`仍`8F+4E`，形成fingerprint `36A4949C...8C635`。 |
| E2l | Tombstone fingerprint短live | **Hard-safety NO-GO；long-run未啟動** | 約2144秒，394 successful／21 failed cycles，10 completed／5round trips，turnover `140.966590`、exact fee `0.01691599080`、net `+0.00283400920`、`+0.2010412 bps`、flat-equity `+0.2010405 bps`、cover `1.1675343`、max DD `0.081617`。行情逆向時以`soft_exit_hard_fallback`持有0.00020，最終自然flat；但其後發生ambiguous submission `1`、reconciliation failure `1`，shutdown仍回報adapter submission unresolved，故即使economics為正也不得promotion。21:39兩次authenticated postflight均process／position／orders `0/0/0`，equity `299.560694`；無需cleanup position，long-run未開始。 |
| E2m | DNS pre-send／optimistic nonce修復 | **Offline repair GO；待T3** | 鑑識定位第23次BUY於`api.rh.lighter.xyz` DNS解析前失敗；舊shared adapter誤將其建為uncertain placeholder。新增預設關閉的MM create opt-in：只接受configured-host精確`ClientConnectorDNSError`，成功回滾單一API key的optimistic nonce後才回報not-sent；OrderManager記`place success=False`、清slot且不增加ambiguity／reconciliation failure。普通timeout、wrong-host、cancel及Grid預設語義不變。決定性測試`8/8`，MM`289/289`；full repo`531`維持既知`8F+4E`。新fingerprint`C4090313...81F017`；本輪未live，需fresh preflight與30分鐘T3。 |
| E2n | 新fingerprint T3與8小時收斂計畫 | **Hard-safety NO-GO；live未啟動** | 22:19:37–22:37:48，約18分鐘；`197/197` cycles、`would_place=394`，真實create/cancel、ambiguity、reconciliation failure、unknown、429、WS reconnect皆`0`，全程flat。Authenticated account read在bounded retries後仍收到CloudFront 502，runtime依trust gate自行退出；不反覆重跑。22:40兩次authenticated postflight皆position／orders`0/0`、equity`299.560694`，config仍為dry-run。 |
| E2o | Account-read retry修復後T3 | **Dry safety GO** | Account audit bounded read改為最多第6次／15秒內恢復，持續失敗仍hard stop。22:55:55–23:28:26，`373/373` cycles；真實create/cancel、failed、ambiguity、reconciliation failure、unknown、429、WS reconnect與account-read failure皆`0`，全程flat。Evidence `logs/market_maker_t3_502_recovery_20260826-232823.log`；graceful stop、runtime0，雙authenticated postflight均position／orders`0/0`。 |
| E2p | 250-tick短live與cancel terminal事件 | **經濟正訊號／正式gate未完成；安全停止** | 23:29:30–23:53:36；最後完整flat checkpoint為18 completed，turnover`312.251260`、exact fee`0.03747015120`、net`+0.00670984880`、`+0.214886 bps`、flat-equity`+0.214891 bps`、cover`1.17907`。一筆cancel tx ack超出舊`4×0.5s`初始proof window，記`ambiguous_cancellations=1/failed_cycles=1`；同order其後以兩筆maker fill取得exact terminal，約0.9秒內解除manager uncertainty，無duplicate／unknown／reconciliation failure。依當時累計counter gate停止，未達30且未啟動long-run。Evidence `logs/market_maker_short_live_ambiguous_cancel_20260826-235332.log`。Stop signal前一張bid成交，最終open orders0、BTC long`0.00020 @ 78017.3`、`1x cross`；runtime0、兩次postflight一致。 |
| E2q | Cancel uncertainty truth與metrics修復 | **Offline/regression GO；等待flat** | 取消無sequence證據的10秒active tombstone豁免；OPEN／PARTIAL保留`UNCERTAIN_CANCELLATION`；manager slot與公開adapter registry依symbol/order key合併去重，只有exact `FILLED/CANCELED/REJECTED/EXPIRED`清marker；startup／shutdown同樣fail closed。Fill generation與full/partial counters只在manager驗證新fill後增加，invalid PENDING cancel placeholder不再誤判。MM`298/298`、Lighter integration`26/26`、精準adapter`3/3`；full repo`542`維持既知`8F+4E`，py_compile／diff-check通過。Current fingerprint`582FE57E...DA0BFC`、source config SHA`9E86A4F4...5B34102`且`dry_run:true`。00:25 authenticated read仍為long`0.00020`／orders0，runtime0；未獲fresh recovery授權，故不啟動T3/live。 |
| E2r | 授權recovery與cancel-registry正式T3 | **Recovery + dry safety GO** | 00:32:38單筆`POST_ONLY + reduce_only SELL 0.00020 @ 78071.0`取得exact fill並flat；雙preflight 0/0。00:33:33–01:04:05 T3為`351/351` cycles、`would_place=702`，真實create/cancel、failed、ambiguity／unresolved、reconciliation failure、unknown、429、WS reconnect與account-read failure皆0，全程flat。Evidence `logs/market_maker_t3_cancel_registry_20260827-010405.log`；graceful stop、runtime0、雙postflight 0/0，equity`299.574211`。 |
| E2s | 250-tick正式60分鐘短gate | **Runtime/safety GO；volume/economic NO-GO；long-run未啟動** | 01:04:46–02:05:03，`726/726` cycles、5 maker fills、2 completed round trips／4 completed fills、completed turnover`62.429240`、exact fee`0.00749150880`、net`+0.00014849120`、`+0.0237855 bps`、cover`1.019821`、max DD`0.106725`；hard-safety counters皆0，shutdown cancellation由新bounded exact-terminal confirmation收斂。第三筆sell遇趨勢上行後長時間停在fee-preserving reduce-only bid，截止仍short`0.00020`，故未達30、未自然flat、無flat-equity proof。Evidence `logs/market_maker_short_live_gate_final_20260827-020503.log`。授權cleanup以`POST_ONLY + reduce_only BUY 0.00020 @ 78425.8` exact fill；雙postflight process／position／orders`0/0/0`、equity`299.495952`，config恢復dry-run。 |
| E2t | 趨勢入場guard＋有界session-loss maker exit | **Code／offline／dry safety GO；live未執行** | 新增external-BBO `60s/125 ticks`方向guard，只擋逆勢風險增加側，reduce-only豁免；方向式50% hysteresis支援解除與直接反轉。`0.10 USDG`maker-exit budget只供latched `POST_ONLY reduce-only`，採completed trade與同generation最後flat-equity較差證據，缺失／stale時hard fallback、超限hard stop，`bounded_economic_recovery`不算GO。交叉審查在最終場前抓出abrupt-reversal與flat-evidence generation兩個邊界並補回歸；受影響`185/185`、MM`315/315`，full repo`559`仍為既知Grid `8F+4E`。Final fingerprint`43E2A1D5...518E7A`、config SHA`27266EF3...EC5463`；20:55:38–21:11:33 T3 `182/182`、would-place`243`，三態多次切換，真實mutation與全部hard-safety counters`0`，flat。Evidence `logs/market_maker_t3_trend_loss_guard_final_20260827-211134.log`；graceful stop、runtime0、雙postflight position／orders`0/0`，source仍dry-run。 |

| E2u | 固定邊界短live | **Hard-safety NO-GO／positive economic signal；long-run未啟動** | 21:41:59–21:47:37，約5分38秒；final audit `83 cycles / 82 successful / 1 failed`、`12 completed / 5 round trips`、turnover`190.098420`、exact fee`0.02281181040`、net`+0.00500818960`、completed／flat-equity`+0.263452 / +0.263442 bps`、cover`1.21954`、max DD`0.023630`，約`127 completed fills/h`。21:47:29一筆partial-fill附近cancel尚無exact terminal proof，current unresolved為`1`且strategy position／authenticated ledger短暫不一致，依hard gate立即Ctrl+C；shutdown exact sync後ambiguity／resolved／unresolved=`1/1/0`、自然flat，unknown／reconciliation failure／429／WS=`0`。Evidence `logs/market_maker_fixed_short_live_hard_stop_20260827-214750.log`；runtime0，雙postflight position／orders`0/0`、equity`299.500960`，config恢復dry-run。未達30，且`session_loss_for_maker_exit=0`，不得promotion或宣稱loss-budget live proof。 |
| E2v | Cancel-vs-fill exact outcome修復 | **Offline/regression GO；待T3** | 針對E2u事件，保留legacy REST bool與Grid adapter contract，新增僅由MM bootstrap啟用的exact terminal side-channel。Cancel ack後若history exact `FILLED`，OrderManager驗證symbol／side／exchange-id或client-id alias／visible fill，清slot、要求position refresh、當周期不replacement，cancel action為`success=false`；同周期未曾公開ambiguous，故不增加resolved counter。Stored FILLED與stale CANCELED衝突、proof無法被owner確認或真正無proof皆保留marker並fail closed；shutdown會傳遞fill effect。精準`7/7`、Lighter cancellation`31/31`、Grid targeted`6/6`、MM`319/319`；full repo`566`維持既知`8F+4E`，Grid production未修改。12-file fingerprint`EE1514C5...E7BAB4`、config SHA`27266EF3...EC5463`且dry-run；22:23雙authenticated reads皆position／orders`0/0`、equity`299.500960`，runtime0。本輪未live。 |
| E2w | Cancel-vs-fill修復後正式T3 | **Dry safety GO；live未授權** | 22:31:15–23:02:40，`360/360` cycles、`would_place=404`；真實create/cancel、failed、ambiguity／unresolved、reconciliation failure、unknown、mutation blocks、429、WS reconnect與account-read failure皆`0`，全程flat。完整13-file fingerprint為`200159C7...1ABCE`；E2v的12-file值只因證據manifest漏列已存在的`core/adapters/exchanges/exceptions.py`，非code drift。Evidence `logs/market_maker_t3_cancel_race_20260827-230233.log`；graceful stop、runtime0，雙authenticated postflight position／orders`0/0`、equity`299.500960`，config SHA仍為`27266EF3...EC5463`且`dry_run:true`。未啟動live。 |
| E2x | Cancel-race修復後固定邊界短live | **Runtime/safety GO；volume/economic NO-GO；long-run禁止** | 23:09:02–00:09:28，`735/735` cycles、21 completed／10 round trips、turnover`322.806772`、exact fee`0.03873681264`、gross`-0.001964`、net`-0.04070081264`、completed／flat-equity`-1.260841 / -1.260816 bps`、max DD`0.138115`、約`320 USDG/h`／`20.84 fills/h`。Short`0.00030`曾長時間停在`soft_exit_hard_fallback`，之後自然flat；session loss`0.04070081264`低於`0.10`budget，但gross未cover fee且未達30。Failed、ambiguity／unresolved、reconciliation failure、unknown、429與WS皆0；兩次read failure由bounded retry吸收。Evidence `logs/market_maker_cancel_race_short_live_20260828-000923.log`；graceful stop、runtime0、雙postflight position／orders`0/0`、equity`299.460260`，source與fresh live copy都恢復dry-run。 |
| E2y | Hummingbot ping-pong最小移植 | **Offline/regression GO；待T3** | 不遷移Hummingbot runtime，只借同側抑制：authenticated position非零時僅保留反向`POST_ONLY + reduce_only`，並要求fill generation與最新authenticated ledger同時flat後才重開雙邊，堵住audit-nonflat→position先flat的競態。全域filled-order delay會連退出側一起延遲，故不採用；hanging-order tracker、Controller／Executor、EpisodeLedger與新mutation lane亦未加入。精準`4/4`、MM`323/323`；full repo`570`維持既知Grid`8F+4E`。Example預設關閉；本地test YAML啟用且`dry_run:true`，SHA`766DD508...F7510806`。Runtime0，本輪未做live mutation。 |
| E2z | Ping-pong T3與固定邊界短live | **T3 GO；live hard-safety／economic NO-GO；long-run禁止** | Fingerprint`4565ECB0...B659EF`、source SHA`766DD508...F7510806`。20:16:00–20:46:25 T3為`348/348` cycles、`would_place=447`，真實mutation與全部hard-safety counters`0`，全程flat；evidence `logs/market_maker_t3_ping_pong_20260828-204622.log`。20:48:12 live啟動並多次完整證明entry後同側抑制、單一反向reduce-only exit、authenticated-flat barrier與恢復報價。21:34:02同一受管order在cancel terminal confirmation清slot後才收到延遲partial-fill／nonterminal WS update，active-slot-only matching誤判unknown；counter`2`是同一事件在order-update與sync路徑各計一次，非兩張外部單，REST／audit在10秒內證明ownership並收斂。依累計hard gate仍於21:36 graceful stop。Final audit`654/654` cycles、34 completed／16 round trips、completed turnover`485.272502`、fee`0.05823270024`、gross`-0.009290`、net`-0.06752270024`、`-1.391439 bps`、cover`-0.15953`、max DD`0.078506`；ambiguity／unresolved／reconciliation failure／429／WS為0。Evidence `logs/market_maker_ping_pong_short_live_unknown_orders_20260828-213627.log`。Runtime0、雙postflight open orders0，但保留BTC long`0.00020`；未獲本輪recovery授權，兩份config均恢復dry-run。 |
| E2aa | E2z殘倉授權recovery | **Maker-only recovery GO；帳戶flat** | 21:48使用既有單向helper處理BTC long`0.00020`：唯一允許方向為SELL，強制`POST_ONLY + reduce_only`，沒有bid／taker／IOC／market路徑；helper取得flat結果並正常退出。21:48:27與21:48:35兩次authenticated postflight均position／open orders=`0/0`、used collateral=`0`，runtime／recovery process=`0`；source與ignored live config均維持`dry_run:true`。 |
| E2ab | Terminal replay ownership與strict-exit候選 | **Offline/regression GO；待fresh T3** | 針對E2z，只在MM OrderManager保留runtime-lifetime terminal ownership proof，key明確區分side／order-id／client-id namespace；延遲非終態WS須同時匹配identity、amount與price。Remaining採單調low-watermark，terminal後才到達的新partial只回報一次position refresh，同一replay不重複fill generation；slot不復活。Foreign／錯side／跨namespace／金融欄位衝突及REST active-after-terminal仍fail closed。沒有TTL、reorder buffer、新ledger、shared adapter或Grid變更。本地source只把`max_session_loss_for_maker_exit`從`0.10`改為`0`，恢復既有fee／authenticated-surplus aware exit，`dry_run:true`、SHA`9162163C...84DF711`。Terminal精準`4/4`、order manager`87/87`、經濟分支`4/4`、MM`326/326`；full repo`573`維持既知Grid`8F+4E`，py_compile／diff-check通過。Runtime0，本輪未live；需新fingerprint T3後才可考慮短場。 |
| E2ac | Terminal replay修復後正式T3 | **Dry safety GO；live未授權** | Fingerprint`984DD3D5...D2AAB`、source SHA`9162163C...84DF711`。00:08:13–00:38:39執行唯一dry-run；保存證據時為`348/348` cycles、`would_place=378`，真實create/cancel、failed、ambiguity／unresolved、reconciliation failure、unknown、blocks、429、WS reconnect與account-read failure皆`0`，全程position／orders=`0/0`。Evidence `logs/market_maker_t3_terminal_replay_20260829-003823.log`；graceful stop、runtime0，00:39兩次authenticated postflight皆identity／endpoint PASS、position／orders=`0/0`、used collateral=`0`、equity`299.372634`。Source維持`dry_run:true`，未啟動live。 |
| E2ad | Terminal replay修復後固定邊界短live | **Runtime／safety／economic GO；long-run未授權** | Fingerprint`984DD3D5...D2AAB`。00:44:19–01:04:29執行唯一短live；final audit為`277/277` cycles、32 completed／16 round trips、turnover`498.137240`、exact fee`0.05977646880`、gross`0.071120`、net`+0.01134353120`、completed／flat-equity`+0.227719 / +0.227708 bps`、cover`1.18977`、max DD`0.011374`。Failed、ambiguity／unresolved、reconciliation failure、unknown、429、WS與account hard stop皆`0`；一次mutation limiter block不構成安全失敗。30 fills且flat時已達GO；01:04:09一次position snapshot unavailable令runtime fail closed，account audit仍healthy，graceful-stop窗口中的maker episode自然完成並由final audit確認flat。Session-scope loss-budget exit與bounded recovery皆`0`，soft exit僅在authenticated surplus為正時生效。Terminal replay未自然觸發，故live未證明該分支。Evidence `logs/market_maker_terminal_replay_short_live_final_20260829-010429.log`；runtime0，雙authenticated postflight position／orders=`0/0`、used collateral=`0`、equity`299.383977`；source與fresh live copy均恢復`dry_run:true`。 |

## 使用規則

- 要追原12.1–12.19的事故細節、時間線、commit、SHA、測試數或歷史postflight時，讀上列Git原文；12.20以後與E2系列以本索引、其後commit及保存的log證據為準。不要把全部歷史複製回主指南。
- 要決定能否啟動、測多久、何時停或下一個candidate時，只使用目前主指南與fresh authenticated reads。
- 歷史GO只覆蓋當時fingerprint與證據範圍；後續parser／runtime修復不會把舊事故改判，也不會自動授權live。
- VPS同步與測試在上述歷次驗證中均不屬於完成範圍。
