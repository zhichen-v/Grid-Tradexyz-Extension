# Runtime dependencies (Python 3.12)
injector==0.24.0
PyYAML==6.0.3
rich==15.0.0

# HTTP and WebSocket clients
aiohttp==3.14.3
# Keep aiohttp DNS resolution working on Windows (pycares 5 breaks aiodns 3.x,
# while aiodns 4 fails to discover the local DNS servers on this environment).
aiodns==3.5.0; sys_platform == "win32"
pycares==4.11.0; sys_platform == "win32"
httpx==0.28.1
python-socketio==5.16.4
requests==2.34.2
websockets==17.0.1

# Exchange clients
# ccxt 4.5.65+ pins urllib3>=2.6, while lighter-sdk 1.1.2 requires urllib3<2.1.
ccxt==4.5.64
eth-account==0.13.7
hyperliquid-python-sdk==0.24.0
lighter-sdk==1.1.2
PyNaCl==1.6.2
